# ===== Service Identity =====
spring.application.name=results-service
server.port=${PORT:8088}

# ===== Security =====
# Set to true to disable authentication (for local testing only!)
security.test-mode=${SECURITY_TEST_MODE:true}

# ===== Database (PostgreSQL) =====
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/results_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false

# ===== OAuth2 Resource Server (JWT validation) =====
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI:http://localhost:8080/oauth2/jwks}

# ===== External Service URLs =====
announcement.api.base-url=${ANNOUNCEMENT_API_BASE_URL:http://localhost:8085}
portfolio.api.base-url=${PORTFOLIO_API_BASE_URL:http://localhost:8084}

# ===== CORS =====
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:8082,https://stockanalytics-hv70.onrender.com}

# ===== PDF Processing =====
pdf.download.timeout-seconds=30
pdf.parse.max-pages=10

# ===== Caching =====
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=500,expireAfterWrite=300s

# ===== Actuator / Monitoring =====
management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.endpoint.health.show-details=when-authorized
management.metrics.tags.application=results-service
management.tracing.sampling.probability=1.0
management.tracing.propagation.type=b3

# ===== Logging =====
logging.level.com.example.results=INFO
logging.level.technology.tabula=WARN
